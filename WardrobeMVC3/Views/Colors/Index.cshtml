@model IEnumerable<WardrobeMVC3.Models.Color>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Color1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Color1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ColorsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ColorsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ColorsID })
        </td>
    </tr>
}

</table>
