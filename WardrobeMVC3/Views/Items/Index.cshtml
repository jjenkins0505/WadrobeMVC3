@model IEnumerable<WardrobeMVC3.Models.Item>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Item1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Item1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemsID })
        </td>
    </tr>
}

</table>
